{
  "sample_id": "4",
  "session_id": "",
  "bug_id": "fac-context-lease-004",
  "task_type": "bugfix",
  "bug_category": "context相关问题",
  "repo_url": "",
  "go_version": "1.23-alpine;1.23.0;auto",
  "repro_determinism": "deterministic",
  "user_query": "worker 这里有个怪事，context 已经取消了还在租任务，等它跑完才停。代码当前目录就能跑，帮我修一下这个取消不生效的问题。",
  "trajectory": "",
  "verify_cmds": "go test ./internal/worker/runner -run '^TestRunnerHonorsCanceledContext$' -count=1\ngo test ./internal/worker/runner -run '^TestRunnerRejectsInvalidLimit$' -count=1\ngo test ./internal/worker/runner -run '^TestRunnerRequiresOwner$' -count=1\ngo test ./internal/worker/runner -run '^TestSchedulerHasTask$' -count=1\ngo test ./internal/worker/runner -run '^TestRunnerMaxConcurrency$' -count=1",
  "gold_root_cause": "文件: internal/protocol/domain/schedule.go,internal/worker/runner/runner.go 符号: Scheduler.LeaseContext,Runner.Run 机制: context 取消传播断裂，已取消 ctx 仍租用任务",
  "gold_patch": "",
  "success_criteria": "1. 基线传入已取消 context 跑 20 次，每次都错误租到任务；2. 修复后同一用例重复 20 次都返回 0 个任务；3. 调度器状态未被取消路径弄脏；4. 撤掉上下文传递或调度器检查会重新红，完整回归通过。",
  "verify_result": "",
  "harness": "",
  "generator_model": "",
  "worker": "hutu",
  "creator": "hutu",
  "qc_result": "",
  "qc_note": "",
  "sync_feishu": "",
  "coverage_contract": {
    "version": 1,
    "claims": [
      {
        "id": "canceled_context_no_lease",
        "description": "已取消 context 不得租用任务",
        "test": "TestRunnerHonorsCanceledContext",
        "command": "go test ./internal/worker/runner -run '^TestRunnerHonorsCanceledContext$' -count=1",
        "paths": [
          "internal/protocol/domain/schedule.go",
          "internal/worker/runner/runner.go"
        ]
      },
      {
        "id": "invalid_limit_rejected",
        "description": "非法 limit 必须返回错误",
        "test": "TestRunnerRejectsInvalidLimit",
        "command": "go test ./internal/worker/runner -run '^TestRunnerRejectsInvalidLimit$' -count=1",
        "paths": [
          "internal/worker/runner/runner.go"
        ]
      },
      {
        "id": "empty_owner_rejected",
        "description": "空 owner 必须返回错误",
        "test": "TestRunnerRequiresOwner",
        "command": "go test ./internal/worker/runner -run '^TestRunnerRequiresOwner$' -count=1",
        "paths": [
          "internal/worker/runner/runner.go"
        ]
      },
      {
        "id": "scheduler_has_task",
        "description": "调度器能正确判断任务存在",
        "test": "TestSchedulerHasTask",
        "command": "go test ./internal/worker/runner -run '^TestSchedulerHasTask$' -count=1",
        "paths": [
          "internal/protocol/domain/schedule.go"
        ]
      },
      {
        "id": "runner_max_concurrency",
        "description": "Runner 返回合理最大并发",
        "test": "TestRunnerMaxConcurrency",
        "command": "go test ./internal/worker/runner -run '^TestRunnerMaxConcurrency$' -count=1",
        "paths": [
          "internal/worker/runner/runner.go"
        ]
      }
    ]
  }
}